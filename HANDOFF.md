# HANDOFF — croatia-site

> Снимок «где остановились» для следующей сессии. Обновляется после каждой доработки.

**Дата:** 2026-06-22 · **Ветка:** `main` · **Сборка:** 53 страницы · `check`/`build`/`test`/`test:links`/`lint` — зелёные, `qa`=GO.

## Где остановились
**Витрина (ShowcaseRail) → чистая CSS-крутилка (2026-06-22).** Движение ленты на главной перенесено с JS (`scrollLeft`+rAF) на CSS-анимацию в самом компоненте — деплоится вместе со страницей, кэш `/js/showcase-rail.js` больше ни на что не влияет. 3 изменения по спеке `_fixes/showcase-marquee-fix.md`: (1) `ShowcaseRail.astro` рендерит **2 копии** набора (`cells = [...items(clone:false), ...items(clone:true)]`; клоны `aria-hidden`, фокус-элементы `tabindex=-1`); `.showcase__track` получил `animation: showcase-marquee var(--showcase-dur,60s) linear infinite` + `@keyframes` `translateX(0 → -50%)` (= ровно одна копия → бесшовно); пауза `animation-play-state:paused` на `:hover`/`:focus-within`; `.showcase__viewport` `overflow-x:auto` → `overflow:hidden`; в `@media (prefers-reduced-motion: reduce)` добавлено `.showcase__track{animation:none;transform:none}` + `.showcase__viewport{overflow-x:auto}` (fallback на прокрутку). (2) `public/js/showcase-rail.js` заменён минимальной версией: только задаёт `--showcase-dur` ∝ ширине набора (`scrollWidth/2`, ~40 px/с, min 20s) + тач-тап раскрывает поповер карточки-заведения; вся прежняя JS-механика (клонирование, drag, wrap, rAF) удалена. Проверка dist (`/` и `/ru/`, ссылаются на один бандл `_astro/HomePage.*.css`): `animation:showcase-marquee` есть, `@keyframes showcase-marquee` есть (минификатор свернул в `translate(-50%)`), `showcase__viewport{overflow:hidden}` есть, `animation-play-state:paused` есть, reduced-motion `animation:none` есть, 5 `aria-hidden` клонов-ячеек на странице (2 копии набора). Гейты: `build`(53)/`check`(0/0)/`test`(паритет en/ru)/`test:links`(2679)/`lint` — все зелёные.

_Ранее (2026-06-22): KALENDAR P0 «бюджет/стоимость» — статья `croatia-trip-cost`._ **KALENDAR P0 «бюджет/стоимость» — статья `croatia-trip-cost` (2026-06-22).** Опубликована пара `croatia-trip-cost` (en ведущий ~1517 слов + ru, паритет), `category=planning` → `/planning/croatia-trip-cost/` и `/ru/planning/croatia-trip-cost/`. Это **первая статья категории `planning`** в коллекции `articles` (раздел `/planning/` существовал хабом). H1: `Is Croatia Expensive? Trip Cost & Budget (2026)` / ru `Дорого ли в Хорватии? Бюджет поездки 2026`. 7 уникальных CC/CC0-фото с Wikimedia (cover — аэро-Дубровник, Akira Takiguchi; монета €2 Хорватии, European Commission; паром Jadrolinija, Quintin Soloviev; рынок Дубровника, José Sáez; ресторанная терраса, Clarenciaga; стены Дубровника, Marcin Konsek; Плитвице, Seraksin CC0), webp ≤200КБ, отобраны глазами. Лид = прямой ответ (дороже Балкан-соседей, дешевле Зап. Европы; €55–75 / €110–160 / €200+ в день). Разделы: дневной бюджет (таблица 3 профиля) → жильё → еда (рынки/пекарни/рестораны) → транспорт/паромы/аренда → билеты (Плитвице/стены Дубровника/Dubrovnik Pass) → сезонность → как сэкономить → перелинк. Без FAQ. **Цены — только ориентиры + «уточняйте/сверяйте» + дисклеймер с датой проверки 22.06.2026** (правило 4: ничего не выдумано как точная цифра), `sources` (турборд, ЕЦБ, Jadrolinija, Плитвице, стены Дубровника). AffiliateBox `trip-hotels`+`trip-carhire` через `/go/`. Перелинк (двусторонний кластер): из статьи → хаб `/planning/` ×2, Сплит, Загреб, Дубровник, маршрут `dalmatia-split-to-dubrovnik`, `/car-rental/`, `/transport/`, виза-гайд `/relocation/`; **встречные ссылки на бюджет-статью добавлены в `split-travel-guide` и `dubrovnik-travel-guide`** (en+ru, строка «Деньги/Money»). **Добавлена 5-м пиком в `showcasePicks` главной** (`category=planning`, kicker `guide`). hreflang en↔ru + x-default взаимные в dist. Гейты: `check`(0/0)/`build`(53 стр.)/`test`/`test:links`/`lint` зелёные, `qa`=GO. **Замечание:** `scripts/new-content.mjs` `ARTICLE_CATEGORIES` не включает `planning` → скаффолд planning-статьи падает (пара создана вручную по CONTENT_GUIDE); стоит дописать `planning` в enum скрипта. Добор перелинковки: при выходе `best-time-to-visit-croatia`, `is-croatia-expensive`, `croatia-hidden-costs`, `croatia-vs-greece-vs-italy` — заменить ссылку-на-хаб `/planning/` на прямые (en/ru).

_Ранее (2026-06-22): KALENDAR Неделя 1 — первый МАРШРУТ по Далмации._ **KALENDAR Неделя 1 — первый МАРШРУТ по Далмации (2026-06-22).** Опубликована пара `dalmatia-split-to-dubrovnik` (en ведущий + ru, паритет), коллекция `routes` → `/routes/dalmatia-split-to-dubrovnik/` и `/ru/routes/dalmatia-split-to-dubrovnik/`. Это **первый материал в коллекции `routes`** (была пустой). Остановки: **Сплит → Трогир → Хвар (паром) → Корчула (паром) → Стон → Дубровник**; `days:6` / `distanceKm:280` / `budgetFrom:€90` / `bestSeason: май, июнь, сентябрь, октябрь`. **Фото каждой из 6 остановок** (`stops[].photo`, 640×360) + cover (панорама Дубровника) + 2 инлайн-figure (мост Пелешац; стены Стона) + 1 gallery (гавань Хвара) — 10 уникальных CC-кадров с Wikimedia, webp ≤200КБ, отобраны глазами (Трогир переснят: первый кандидат оказался фото таблички). Морские участки (Сплит→Хвар, Хвар→Корчула) отмечены как паромы Jadrolinija/Krilo в тексте и таблице участков. Геометрия запечена `node scripts/build-route-geometry.mjs` (143 точки, OSRM driving). Тело: лид → по остановкам (что посмотреть, ночёвки, паромы/расстояния) → «как сделать: машина/паром/оба» + таблица участков + сезон. Перелинк (двусторонний кластер): из маршрута → Сплит-гайд, Дубровник-гайд, новость о паромах Сплита, хаб `/routes/`; встречные ссылки на маршрут добавлены в Сплит-гайд и Дубровник-гайд (en+ru). AffiliateBox — `trip-carhire` + `trip-hotels` через `/go/` (встроены в `RoutePage.astro`). hreflang en↔ru + x-default, TouristTrip-schema в dist. Гейты зелёные, `qa`=GO. Добор перелинковки: при выходе `split-to-hvar-ferry`, `hvar-island-guide`, `croatia-ferries-guide`, `one-way-car-rental-croatia` — добавить прямые ссылки (en/ru).

_Ранее (2026-06-22): витрина (ShowcaseRail) — починена непрерывная крутилка._ Золотая лента на главной теперь крутится БЕСШОВНО ПО КРУГУ и не ставит одинаковые карточки рядом даже при немногих пиках. Было: статический двойной рендер карточек в `ShowcaseRail.astro` + старый JS, который заворачивал `scrollLeft` на `scrollWidth/2` — при малом числе карточек лента упиралась в край и «стояла»/дёргалась. Стало: (1) `public/js/showcase-rail.js` заменён эталоном — JS сам клонирует уникальный набор до ≥2.5× ширины вьюпорта и заворачивает петлю ровно на ширину одного набора (`wrap`), клоны `data-clone`+`aria-hidden`+`tabindex=-1`; пауза/спотлайт/drag/тач/reduced-motion сохранены (делегирование событий); (2) `ShowcaseRail.astro` — одинарный рендер `cells = items.map((it) => ({ ...it, clone: false }))` (в разметке `data-clone`=0, без дублей); (3) `HomePage.astro` — `showcasePicks` расширен 4-м реальным пиком `croatia-digital-nomad-visa` (`category=relocation`, kicker `guide`) + дедуп по slug; новый kicker `guide` (en/ru) в `i18n/{types,en,ru}.ts`. Набор витрины: `zagreb-travel-guide`, `dubrovnik-travel-guide`, `split-travel-guide` (cities) + `croatia-digital-nomad-visa` (relocation). В dist `/` и `/ru/` — витрина есть, 4 уникальные карточки, `data-clone`=0. Гейты: `build`(49)/`check`(0/0)/`test`/`test:links`(2415)/`lint` зелёные.

_Ранее (2026-06-22): KALENDAR Неделя 2 — статья-город Сплит._ Опубликована пара `split-travel-guide` (en ведущий ~1707 слов + ru, паритет), `category=cities` → `/cities/split-travel-guide/` и `/ru/cities/split-travel-guide/`. 12 уникальных фото CC с Wikimedia Commons (cover + 8 инлайн-figure + 3 gallery), webp ≤200КБ, отобраны глазами, `coverCredit` проставлен. Содержание: дворец Диоклетиана (UNESCO, ворота, субструкции/«Игра престолов»), Перистиль + собор Св. Домния + колокольня, Рива + рынки Пазар/Рибарница, холм Марьян + пляж Бачвице, дейтрипы паромом Jadrolinija (Хвар/Брач–Златни-Рат/Вис/Трогир/Крка), где жить, аэропорт SPU (Каштела ~24 км) + паромный хаб, где поесть, практика. `geo`/`hotelWidget:true`/`accessFrom`/`visit`; AffiliateBox `trip-hotels`+`trip-tours` через `/go/`. Перелинк: хаб `/cities/` + Загреб + Дубровник + свежая новость о паромах Сплита + хабы `/transport/ /car-rental/ /planning/ /routes/ /food/`. Добор перелинковки: при выходе `day-trips-from-split`, `split-to-hvar-ferry`, `10-day-croatia-itinerary`, `hvar-island-guide`, `krka-national-park` заменить ссылки-на-хабы на прямые (en/ru). Опубликована пара `split-travel-guide` (en ведущий ~1707 слов + ru, паритет), `category=cities` → `/cities/split-travel-guide/` и `/ru/cities/split-travel-guide/`. 12 уникальных фото CC с Wikimedia Commons (cover + 8 инлайн-figure + 3 gallery), webp ≤200КБ, отобраны глазами, `coverCredit` проставлен. Содержание: дворец Диоклетиана (UNESCO, ворота, субструкции/«Игра престолов»), Перистиль + собор Св. Домния + колокольня, Рива + рынки Пазар/Рибарница, холм Марьян + пляж Бачвице, дейтрипы паромом Jadrolinija (Хвар/Брач–Златни-Рат/Вис/Трогир/Крка), где жить, аэропорт SPU (Каштела ~24 км) + паромный хаб, где поесть, практика. `geo`/`hotelWidget:true`/`accessFrom`/`visit`; AffiliateBox `trip-hotels`+`trip-tours` через `/go/`. Перелинк: хаб `/cities/` + Загреб + Дубровник + свежая новость о паромах Сплита + хабы `/transport/ /car-rental/ /planning/ /routes/ /food/`. **Сплит добавлен 3-м пиком в `showcasePicks` главной** (чип Split→Сплит локализован в CITY_RU). Гейты зелёные, `qa`=GO. Добор перелинковки: при выходе `day-trips-from-split`, `split-to-hvar-ferry`, `10-day-croatia-itinerary`, `hvar-island-guide`, `krka-national-park` заменить ссылки-на-хабы на прямые (en/ru).

_Ранее (2026-06-22): витрина главной заполнена городами._ `showcasePicks` в `src/layouts/HomePage.astro` ссылался на несуществующий контент → лента была пустой. Заполнен опубликованными городами (`zagreb-travel-guide`, `dubrovnik-travel-guide`, теперь + `split-travel-guide`, `category=cities`); чип города локализован для ru. Тип массива расширен до union, чтобы спящие ветки restaurant/route оставались валидными. Витрина показывается на `/` и `/ru/`; LangSwitcher — ровно en+ru.

**Удалён украинский язык (uk) — 2026-06-22 (решение владельца).** Сайт стал 2-язычным: **en** (по умолчанию, корень) + **ru** (`/ru/`). Удалены `src/pages/uk/`, контент `*/uk/`, словарь `i18n/uk.ts`; `LANGS=['ru','en']`; из кода (i18n, BaseLayout, Header/Footer/LangSwitcher/RatesBar, все слои, 404) убраны uk-ветки. hreflang теперь en↔ru + x-default=en; sitemap-locales `{en,ru}`; `check-parity` требует пару **en+ru**. В `public/_redirects` добавлен `/uk/* /:splat 301` (существующие слаг-редиректы и `/en/*` сохранены). Гейты зелёные; в dist нет `/uk/`-страниц и `hreflang="uk"`. Доки синхронизированы (CLAUDE/SPEC/CONTENT_GUIDE/PROGRESS/HANDOFF).

_Ранее (R4: контент по KALENDAR, Неделя 1):_ опубликованы две статьи-города **Загреб** (`zagreb-travel-guide`, 15 фото) и **Дубровник** (`dubrovnik-travel-guide`, 11 фото) + первый батч `/news` (2026-06-22, 2 новости): `split-airport-ferry-crowds-june-2026` (транспорт — Сплит 100 000+ пассажиров, доп. рейсы Jadrolinija) и `inmusic-festival-zagreb-2026` (события — INmusic на Яруне 22–24.06.2026, Jack White/Gorillaz/Kings of Leon; перелинк на свежий `/cities/zagreb-travel-guide/`). Факты верифицированы по источникам текущего года (TCN 20.06.2026; офиц. сайт INmusic). В репо **4 статьи-гайда + 2 новости** (пары en/ru). Следующее по KALENDAR: **Вс 28.06 — маршрут island hopping** (Split→Hvar→Korčula→Dubrovnik, routes) и **Пт 03.07 — Ровинь** (cities).

_Ранее (2026-06-22): завершён Этап 1 (тех-аудит, чистка грузинских следов, инфра-доки, стандарты, память, верификация `/news`); движок и запуск-инфраструктура готовы._

**Миграция URL-слагов (2026-06-22):** разделы переведены с русско-транслитных на английские слаги (единая EN-схема сети, эталон — Македония): `/attractions/ /cities/ /food/ /entertainment/ /routes/ /planning/ /transport/ /car-rental/ /insurance/ /relocation/ (+ /relocation/services/) /news/ /about/ /contact/`. Переименованы директории `src/pages` (+ ru-зеркала), enum категорий, i18n, все ссылки, frontmatter статьи, скрипты, sitemap-фильтр; 301-редиректы старых путей в `public/_redirects`. Гейты зелёные: `build` (58 стр.), `check` (0 ошибок), `test:links` (2765 ссылок, 0 битых), `test`, `lint`.

## Что в работе / на паузе
- **Контент** — идёт по KALENDAR (Неделя 1 начата статьёй Загреб).
- **Добор перелинковки Загреба:** ссылки на 7-day-itinerary и best-time пока ведут на хабы `/routes/` и `/planning/` — заменить на прямые ссылки на статьи, когда те выйдут (неделя 2–3), в обоих языках (en/ru).
- **Открытые фиксы** — в `ROADMAP-FIX.md` (приоритезированы).

## Нужно от владельца
- Партнёрские ID/ссылки: **EKTA**-трекинг (Travelpayouts), **DiscoverCars** (авто), позже GetYourGuide/Viator.
- Настроить `www`-редирект в Cloudflare (CNAME `www` + redirect на apex).
- Решить: делать ли страницу политики конфиденциальности (для ссылки из cookie-баннера).
- Источник фактуры для `living-*` и личных цифр («моат»): бриф владельца или веб-первоисточники с пометкой «сверить при публикации».

## Ключевые точки входа
- Стратегия: `docs/STRATEGY.md`, `docs/CROATIA-ANALYSIS-2026-06-20.md`, план: `docs/CONTENT_PLAN.md`.
- Правила: `CLAUDE.md` (СТАНДАРТЫ + ПРАВИЛО ДИСЦИПЛИНЫ), `CONTENT_GUIDE.md`.
- Аудит: `AUDIT-2026-06-22.md`, ledger `AUDIT.md`. Фиксы: `ROADMAP-FIX.md`. Память: `docs/memory/MEMORY.md`.
- Архив движка-донора (НЕ актуально, балласт-образец): `docs/_georgia-reference/`.
